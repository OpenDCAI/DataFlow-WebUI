fastapi[standard]
# fastapi-mcp 0.4.0 still calls the MCP v1 Server(name, description) API.
# MCP 2.x made description keyword-only, which raises a TypeError at startup.
fastapi-mcp==0.4.0
mcp>=1.12,<2
uvicorn[standard]
pydantic_settings
pandas
loguru
ray
open-dataflow==1.0.10
# DataFlow text operators still import pkg_resources. setuptools 82 removed it.
setuptools<82
